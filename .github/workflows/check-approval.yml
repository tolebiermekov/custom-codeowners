name: 🛂 Check PR Approval (CODEOWNERS-DWH)

on:
  pull_request_review:
    types: [submitted]

permissions:
  pull-requests: read
  contents: read

jobs:
  check-approvals:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install Python dependencies
        run: pip install pathspec

      - name: 🕵️ Check Approvals (Python)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python - <<EOF
          import os
          import sys
          import json
          import pathspec
          import urllib.request
          import urllib.error
          
          # --- 0. Helper functions ---
          def fail(message):
              print(f"::error::{message}")
              sys.exit(1)
          
          def make_request(url, headers):
              """Helper to make GitHub API requests using standard library"""
              req = urllib.request.Request(url, headers=headers)
              try:
                  with urllib.request.urlopen(req) as response:
                      if response.status >= 300:
                          fail(f"API request failed with status {response.status}: {response.read().decode()}")
                      return json.loads(response.read().decode('utf-8'))
              except urllib.error.HTTPError as e:
                  fail(f"HTTPError when requesting {url}: {e.code} {e.reason}")
              except Exception as e:
                  fail(f"Failed during request to {url}: {e}")

          # --- 1. Get Context and Headers ---
          TOKEN = os.environ['GITHUB_TOKEN']
          HEADERS = {'Authorization': f'token {TOKEN}', 'Accept': 'application/vnd.github.v3+json'}
          
          try:
              repo_full = os.environ['GITHUB_REPOSITORY']
              owner, repo = repo_full.split('/')
              
              with open(os.environ['GITHUB_EVENT_PATH'], 'r') as f:
                  event_data = json.load(f)
              pr_number = event_data['pull_request']['number']
          except Exception as e:
              fail(f"Failed to get context: {e}")
              
          CODEOWNERS_FILE = '.github/CODEOWNERS-DWH'
          
          # --- 2. Parse the CODEOWNERS-DWH file (with fixes) ---
          rules = []
          try:
              with open(CODEOWNERS_FILE, 'r') as f:
                  for line in f:
                      line = line.strip()
                      if not line or line.startswith('#'):
                          continue
                      
                      parts = line.split()
                      raw_pattern = parts[0]
                      owner_strings = parts[1:]
                      
                      # --- TRIPLE FIX ---
                      pattern = '**' if raw_pattern == '*' else raw_pattern
                      if pattern.endswith('/') and len(pattern) > 1:
                          pattern += '**'
                      if pattern.startswith('/'):
                          pattern = pattern[1:]
                      # --- End Fix ---
                      
                      owners = set(o.replace('@', '') for o in owner_strings if '/' not in o)
                      rules.append({'pattern': pattern, 'owners': owners})
                      
          except FileNotFoundError:
              fail(f"Failed to read {CODEOWNERS_FILE}: File not found.")
          except Exception as e:
              fail(f"Failed to parse {CODEOWNERS_FILE}: {e}")
          
          # --- 3. Get all changed files in PR (using urllib) ---
          try:
              files_url = f'https://api.github.com/repos/{owner}/{repo}/pulls/{pr_number}/files'
              files_json = make_request(files_url, HEADERS)
              changed_files = [f['filename'] for f in files_json]
          except Exception as e:
              fail(f"Failed to get changed files: {e}")
          
          # --- 4. Get all users who clicked 'Approve' (using urllib) ---
          try:
              reviews_url = f'https://api.github.com/repos/{owner}/{repo}/pulls/{pr_number}/reviews'
              reviews_json = make_request(reviews_url, HEADERS)
              
              approved_users = set()
              for review in reviews_json:
                  if review['state'] == 'APPROVED':
                      approved_users.add(review['user']['login'])
                      
              if not approved_users:
                  fail('No "Approve" found on this PR.')
                  
              print(f"Found approvals from: {', '.join(approved_users)}")
          except Exception as e:
              fail(f"Failed to get reviews: {e}")
          
          # --- 5. "Per-File" checking logic (NEW: files without owners will FAIL) ---
          all_files_covered = True
          uncovered_files = []
          
          for file in changed_files:
              last_match_owners = None
              
              # Find owners for this file (Last Match Wins)
              for rule in rules:
                  spec = pathspec.PathSpec.from_lines('gitwildmatch', [rule['pattern']])
                  if spec.match_files([file]):
                      last_match_owners = rule['owners']
              
              if not last_match_owners: # Covers None or empty set {}
                  print(f"File {file} has no owner in CODEOWNERS-DWH. Failing.")
                  all_files_covered = False
                  uncovered_files.append(f"- {file} (has NO owner assigned in CODEOWNERS-DWH)")
                  continue
              
              # Check for intersection
              intersection = approved_users.intersection(last_match_owners)
              
              if not intersection:
                  all_files_covered = False
                  uncovered_files.append(f"- {file} (requires: {', '.join(last_match_owners)})")
              else:
                  print(f"File {file} is covered by approval from: {', '.join(intersection)}")
          
          # --- 6. Final verdict ---
          if all_files_covered:
              print("All changed files are covered by owners.")
          else:
              uncovered_files_str = '\n'.join(uncovered_files)
              error_message = (
                  f"Not all files are covered by approval. "
                  f"Approver(s) {', '.join(approved_users)} do not have permissions for:\n"
                  f"{uncovered_files_str}"
              )
              fail(error_message)
          EOF